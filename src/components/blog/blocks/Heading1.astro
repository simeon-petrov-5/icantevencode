---
import type { Heading1BlockObjectResponse } from "@notionhq/client/build/src/api-endpoints";

interface Props {
  block: Heading1BlockObjectResponse;
}

const { block } = Astro.props as Props;

const headingBlock: Heading1BlockObjectResponse["heading_1"] =
  block[block.type];
---

<h1>{headingBlock.rich_text[0].plain_text}</h1>
